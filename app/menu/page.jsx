import MenuContent from '@/components/MenuContent';

export const metadata = {
  title: 'Menu — WOW! Momo',
  description: 'The full Wow! menu — momos, Chindian, fried chicken and kulfi. Veg, non-veg, spice levels and nutrition for every dish.',
};

export default function MenuPage() {
  return <MenuContent />;
}
